import styled from "styled-components";

export const TrashButton = styled.a`
    text-decoration: none;
    font-size: 1.4rem;
    color: var(--white);

    &:hover {
        color: var(--red);
    }
`